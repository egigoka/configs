---
description: List saved OpenAI OAuth subscriptions
---

Use the `list_openai_accounts` tool from the opencode-openai-sub-switcher plugin.

Workflow:

1. Call `list_openai_accounts`.
2. Show the saved accounts exactly as returned.
3. Mention which one is active if one is marked with `*`.

Important:

- Keep the reply concise.
- After listing, ask the user one short question about what they want to do next.
